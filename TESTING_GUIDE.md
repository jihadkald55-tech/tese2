# 🔧 دليل اختبار نظام تسجيل الدخول وقاعدة البيانات

## ✅ تم التحقق والإصلاح من

تم فحص شامل لـ:

- ✅ نظام المصادقة (UserContext)
- ✅ صفحات تسجيل الدخول والتسجيل
- ✅ نسيان وإعادة تعيين كلمة المرور
- ✅ حماية صفحات Dashboard
- ✅ قاعدة البيانات وJdول
- ✅ Row Level Security
- ✅ جميع الـ Triggers والـ Functions

---

## 🚀 خطوات البدء السريع

### المرحلة 1: إعداد Supabase

```bash
# 1. اذهب إلى: https://supabase.com/dashboard
# 2. انسخ مشروعك أو أنشئ واحداً جديداً
# 3. اذهب إلى SQL Editor
# 4. انسخ جميع محتوى: supabase/schema.sql
# 5. اضغط Run
```

**تحقق من:**

- ✅ 7 جداول تم إنشاؤها
- ✅ جميع الـ RLS مفعلة
- ✅ جميع الـ Triggers جاهزة

### المرحلة 2: الإعداد المحلي

```bash
# في مشروعك
npm install

# تأكد من وجود .env.local
# NEXT_PUBLIC_SUPABASE_URL=your_url
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_key
```

### المرحلة 3: تشغيل المشروع

```bash
npm run dev
# ثم اذهب إلى http://localhost:3000
```

---

## 🧪 سيناريوهات الاختبار

### ✅ سيناريو 1: تسجيل دخول عادي

**الخطوات:**

```
1. اذهب إلى /login
2. أدخل:
   - البريد: student@university.edu
   - كلمة المرور: 123456
3. انقر "تسجيل الدخول"
4. يجب أن ينتقل فوراً إلى /dashboard
```

**النتائج المتوقعة:**

- ✅ رسالة نجاح (من UserContext)
- ✅ البيانات تحملت من قاعدة البيانات
- ✅ Dashboard ظهر بشكل صحيح

### ✅ سيناريو 2: تسجيل حساب جديد

**الخطوات:**

```
1. اذهب إلى /register
2. أدخل:
   - الاسم: محمد أحمد
   - البريد: test@example.com
   - كلمة المرور: Test1234
   - تأكيد: Test1234
   - النوع: طالب
3. وافق على الشروط
4. انقر "إنشاء الحساب"
```

**النتائج المتوقعة:**

- ✅ حساب تم إنشاؤه في Supabase Auth
- ✅ Record تم إنشاء في جدول users
- ✅ انتقل إلى /dashboard

**للتحقق:**

```
في Supabase Dashboard:
1. Authentication → Users
2. تحقق من وجود المستخدم الجديد

3. Table Editor → users
4. تحقق من وجود record للمستخدم الجديد
```

### ✅ سيناريو 3: نسيان كلمة المرور

**الخطوات:**

```
1. اذهب إلى /login
2. انقر "نسيت كلمة المرور؟"
3. يذهب لـ /forgot-password
4. أدخل بريدك الإلكتروني
5. انقر "إرسال رابط إعادة التعيين"
```

**النتائج المتوقعة:**

- ✅ رسالة "تم الإرسال بنجاح"
- ✅ بريد أرسل (تحقق في Supabase Auth → Email)

### ✅ سيناريو 4: إعادة تعيين كلمة المرور

**الخطوات:**

```
# في بيئة التطوير، يمكنك استخدام رابط من Supabase
1. اذهب إلى Supabase Dashboard
2. Authentication → Email Templates
3. انسخ رابط البريد من `Reset Password Link`
4. استخدمه في المتصفح

# أو: انتظر لحين وصول البريد (إذا كان مفعل)
1. افتح البريد
2. انقر على الرابط
```

**النتائج المتوقعة:**

- ✅ ينتقل لـ /reset-password
- ✅ يتحقق من صحة الرابط
- ✅ يعرض نموذج لإدخال كلمة المرور الجديدة

**إدخال كلمة المرور الجديدة:**

```
1. أدخل كلمة مرور جديدة (6 أحرف على الأقل)
2. أدخل التأكيد
3. انقر "حفظ كلمة المرور"
```

**النتائج المتوقعة:**

- ✅ رسالة نجاح مع Checkmark أخضر
- ✅ إعادة توجيه تلقائية لـ /login بعد 3 ثوانٍ
- ✅ يمكنك تسجيل الدخول بالكلمة الجديدة

### ✅ سيناريو 5: حماية Dashboard

**الخطوات:**

```
1. اذهب مباشرة لـ /dashboard
2. بدون تسجيل دخول
```

**النتائج المتوقعة:**

- ✅ يعرض loading state
- ✅ يعيد توجيه لـ /login?redirect=/dashboard
- ✅ بعد تسجيل الدخول ينتقل مباشرة لـ /dashboard

### ✅ سيناريو 6: البيانات الحقيقية

**اختبر:**

```
1. سجل دخول
2. اذهب لـ /dashboard/research
3. أكتب نصاً
4. اضغط حفظ
5. اغلق الصفحة
6. افتح الصفحة مرة أخرى
7. تحقق من أن النص ما زال موجوداً
```

**يجب أن ترى:**

- ✅ البيانات محفوظة في Supabase
- ✅ تحملت البيانات من قاعدة البيانات

---

## 🐛 استكشاف الأخطاء

### إذا حصلت على خطأ "Invalid login credentials"

**الحل:**

```
1. تحقق من أن البريد صحيح غير مكرر
2. تحقق من أن كلمة المرور ليست معيبة
3. في Supabase → Auth → Email Confirmations
   تأكد من تفعيل البريد (إذا كان مطلوباً)
```

### إذا لم تصل رسالة البريد

**في بيئة التطوير:**

```
1. Supabase Dashboard → Auth → Email Templates
2. انسخ الرابط من Reset Password Link
3. استخدمه مباشرة في المتصفح
```

### إذا قال "جدول users غير موجود"

**الحل:**

```
1. تأكد من تنفيذ schema.sql كاملاً
2. في Supabase → SQL Editor → New Query
3. انسخ supabase/schema.sql كاملاً
4. اضغط Run
5. انتظر حتى اكتمل بدون أخطاء
```

### إذا قال "غير مصرح"

**الحل:**

```
1. تأكد من RLS policies معرفة
2. اذهب إلى Supabase → Authentication
3. تأكد من أن auth.uid() يعمل بشكل صحيح
```

---

## 📊 قائمة التحقق النهائية

قبل النشر:

- [ ] تسجيل دخول يعمل
- [ ] تسجيل حساب جديد يعمل
- [ ] نسيان كلمة المرور يعمل
- [ ] إعادة تعيين كلمة المرور يعمل
- [ ] Dashboard محمي
- [ ] البيانات تحفظ وتحمل بشكل صحيح
- [ ] الوضع المظلم يعمل
- [ ] لا توجد أخطاء في Console
- [ ] الرسائل بالعربية ظاهرة بشكل صحيح

---

## 📱 اختبار على أجهزة مختلفة

### على الموبايل:

```
1. في Chrome DevTools
2. Ctrl+Shift+M (أو Cmd+Shift+M)
3. اختبر جميع الصفحات
4. تحقق من أن الشاشة مناسبة
```

### على متصفحات مختلفة:

```
- Chrome ✅
- Firefox ✅
- Safari ✅
- Edge ✅
```

---

## 🎓 ملاحظات مهمة

### آمان:

- ✅ كلمات المرور مشفرة في Supabase
- ✅ لا تخزن في localStorage
- ✅ جميع الاتصالات HTTPS
- ✅ RLS مفعلة على جميع الجداول

### الأداء:

- ✅ Indexes على جميع مفاتيح البحث
- ✅ Lazy loading للبيانات
- ✅ Cache control للـ Static content

### المستقبل:

- ✅ جاهز للـ Multi-language
- ✅ جاهز للـ Dark mode
- ✅ جاهز للتوسع

---

## 🚀 جاهز للنشر!

```bash
# بناء المشروع
npm run build

# اختبر النسخة المنتجة
npm start

# ثم انشره على Vercel:
vercel --prod

# لا تنسى إضافة متغيرات البيئة في Vercel Dashboard
```

---

_دليل الاختبار: 15 فبراير 2026_
