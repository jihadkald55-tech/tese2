# دليل حل مشكلة "جاري تسجيل الدخول"

## 🔴 المشكلة

عند محاولة تسجيل الدخول، الصفحة تبقى في حالة "جاري تسجيل الدخول..." بدون أن تكمل العملية.

## 🎯 السبب الرئيسي

الحسابات الاختبارية المذكورة في صفحة تسجيل الدخول **غير موجودة فعلياً** في Supabase:

- student@university.edu
- prof@university.edu
- admin@university.edu

## ✅ الحل الموصى به (سريع)

### الخطوة 1️⃣: إضافة مفتاح الخدمة (Service Key)

1. اذهب إلى **Supabase Dashboard**
2. اختر مشروعك (duntvpipicwqyhrpdvm)
3. اذهب إلى **Settings** → **API**
4. انسخ **Service role key** (المفتاح الخاص)
5. أضفه في ملف `.env.local`:

```dotenv
SUPABASE_SERVICE_KEY=your_service_key_here
```

### الخطوة 2️⃣: إنشاء الحسابات الاختبارية

1. شغّل السيرفر: `npm run dev`
2. اذهب إلى: **http://localhost:3000/api/setup**
3. ستظهر رسالة بأن الحسابات تم إنشاؤها

### الخطوة 3️⃣: جرّب تسجيل الدخول

استخدم أي من: - البريد: `student@university.edu` كلمة المرور: `123456`

- البريد: `prof@university.edu` كلمة المرور: `123456`
- البريد: `admin@university.edu` كلمة المرور: `123456`

---

## 🔄 الحل البديل 1: إنشاء حساب جديد يدويًا

إذا لم تتمكن من الحصول على Service Key:

1. اذهب إلى صفحة **التسجيل**: http://localhost:3000/register
2. أدخل البيانات:
   - الاسم: أي اسم تريده
   - البريد: أي عنوان بريد صحيح
   - كلمة المرور: أي كلمة قوية
   - اختر نوع المستخدم
3. انقر **"إنشاء الحساب"**
4. استخدم نفس البيانات لتسجيل الدخول

---

## 🔄 الحل البديل 2: الإنشاء مباشرة من Supabase

### إنشاء المستخدمين يدويًا:

1. اذهب إلى **Supabase Dashboard**
2. اختر **Authentication** → **Users**
3. انقر **"Create New User"**
4. أدخل:
   - Email: `student@university.edu`
   - Password: `123456`
   - Confirm: ضع العلامة على "Auto confirm user"
5. كرر للحسابات الأخرى

ثم أضف السجلات في جدول users:

1. اذهب إلى **Table Editor** → **users**
2. أضف صف جديد:

```json
{
  "id": "نفس ID المستخدم من Auth",
  "email": "student@university.edu",
  "name": "محمد الطالب",
  "user_type": "student"
}
```

---

## 🐛 معلومات التصحيح الإضافية

اللتحقق من الخطأ بالضبط:

1. افتح متصفحك وادخل على أدوات المطور (**F12**)
2. اذهب إلى تبويب **Console**
3. حاول تسجيل الدخول
4. انسخ رسالة الخطأ وشارها

---

## ✨ التحسينات التي تمت:

✅ أضيف timeout 15 ثانية لعملية تسجيل الدخول
✅ رسائل خطأ أفضل وأوضح بالعربية
✅ يظهر الآن "انتهت مهلة الانتظار" بدلاً من التعليق
