# 📚 معرض الأبحاث المكتملة - أرشيف مداد

## نظرة عامة

تم إضافة ميزة **"معرض الأبحاث المكتملة"** (Research Gallery) أو **"أرشيف مداد"** كمكتبة إلهام ومرجع معرفي للطلاب الجدد. هذه الميزة تحول نظام مداد من مجرد أداة للكتابة إلى **منصة معرفية** تربط دفعات التخرج ببعضها.

---

## ✨ المميزات الرئيسية

### 1️⃣ معرض الأبحاث (للطلاب)

- **الموقع**: صفحة "استكشف" في القائمة الجانبية (`/dashboard/gallery`)
- **الوصول**: متاح لجميع الطلاب والمشرفين
- **المحتوى**: عرض الأبحاث المنشورة فقط في شكل بطاقات جذابة

**مكونات البطاقة**:

- ✅ عنوان البحث
- ✅ اسم الطالب (صاحب البحث)
- ✅ اسم المشرف
- ✅ سنة التخرج
- ✅ ملخص بسيط للبحث
- ✅ عدد الكلمات
- ✅ علامة "بحث مميز" (للأبحاث الاستثنائية)

### 2️⃣ إدارة النشر (للمشرفين)

- **الموقع**: صفحة "إدارة النشر" في القائمة الجانبية (`/dashboard/publish`)
- **الوصول**: متاح للمشرفين فقط
- **الوظيفة**: ترشيح الأبحاث المكتملة للنشر في المعرض

**خيارات المشرف**:

1. **نشر في المعرض**: نشر البحث ليكون مرئياً لجميع المستخدمين
2. **ترشيح كبحث مميز** ⭐: جعل البحث "جوهرة" مميزة في المعرض
3. **إزالة التمييز**: إعادة البحث لحالة "منشور عادي"
4. **إلغاء النشر**: إخفاء البحث من المعرض

---

## 🗄️ التغييرات في قاعدة البيانات

### جدول `research_projects` - الحقول الجديدة:

```sql
summary TEXT                    -- ملخص البحث
is_published BOOLEAN           -- هل البحث منشور في المعرض
is_featured BOOLEAN            -- هل البحث مميز (جوهرة)
published_at TIMESTAMP         -- تاريخ النشر
supervisor_id UUID             -- معرف المشرف
supervisor_name TEXT           -- اسم المشرف
graduation_year INTEGER        -- سنة التخرج
```

### السياسات الأمنية (RLS):

```sql
-- السماح بعرض الأبحاث المنشورة للجميع
CREATE POLICY "Users can view published research projects"
ON research_projects FOR SELECT
USING (is_published = TRUE);

-- السماح للمشرفين بتحديث الأبحاث المخصصة لهم
CREATE POLICY "Supervisors can update assigned research projects"
ON research_projects FOR UPDATE
USING (auth.uid() = supervisor_id);
```

### الفهارس (Indexes) للأداء:

```sql
CREATE INDEX idx_research_projects_published
ON research_projects(is_published)
WHERE is_published = TRUE;

CREATE INDEX idx_research_projects_featured
ON research_projects(is_featured)
WHERE is_featured = TRUE;
```

---

## 🚀 الملفات الجديدة

### 1. API Endpoints:

#### `/api/gallery` (GET)

**الوظيفة**: جلب الأبحاث المنشورة
**المعاملات**:

- `featured=true`: جلب الأبحاث المميزة فقط
- `year=2026`: التصفية حسب سنة التخرج

**الاستجابة**:

```json
{
  "success": true,
  "projects": [...],
  "count": 10
}
```

#### `/api/publish` (POST)

**الوظيفة**: ترشيح بحث للنشر (للمشرفين فقط)
**البيانات المطلوبة**:

```json
{
  "researchId": "uuid",
  "isPublished": true,
  "isFeatured": false,
  "supervisorId": "uuid",
  "supervisorName": "د. أحمد",
  "graduationYear": 2026,
  "summary": "ملخص البحث..."
}
```

#### `/api/publish` (GET)

**الوظيفة**: جلب الأبحاث المكتملة للمشرف
**المعاملات**:

- `supervisorId=uuid`: جلب أبحاث مشرف معين

### 2. المكونات (Components):

#### `src/app/dashboard/gallery/page.tsx`

صفحة معرض الأبحاث - واجهة جذابة لعرض الأبحاث المنشورة

**المميزات**:

- 🔍 بحث في العناوين والأسماء
- 📅 تصفية حسب السنة
- ⭐ عرض المميزة فقط
- 📊 إحصائيات عامة
- 🎨 تصميم بطاقات أنيق
- 💬 نافذة تفاصيل منبثقة

#### `src/components/PublishManager.tsx`

لوحة تحكم المشرف لإدارة النشر

**المميزات**:

- 📋 عرض جميع الأبحاث المكتملة
- ✅ نشر/إلغاء النشر
- ⭐ ترشيح كمميز
- 📊 إحصائيات النشر
- 🔔 إرسال إشعارات للطلاب

#### `src/app/dashboard/publish/page.tsx`

صفحة إدارة النشر (wrapper)

### 3. تحديثات Sidebar:

**للطلاب والمشرفين**:

- ➕ زر "استكشف" (أيقونة Award)

**للمشرفين فقط**:

- ➕ زر "إدارة النشر" (أيقونة Sparkles)

---

## 🎯 طريقة الاستخدام

### للطلاب:

1. **استكشاف الأبحاث**:
   - انقر على "استكشف" في القائمة الجانبية
   - تصفح الأبحاث المنشورة
   - استخدم البحث والفلاتر للعثور على أبحاث معينة
   - انقر على أي بحث لعرض التفاصيل الكاملة

2. **الفائدة**:
   - معرفة مستوى الأبحاث المطلوب
   - الاستفادة كمرجع للدراسات السابقة
   - الإلهام من الأبحاث المميزة

### للمشرفين:

1. **الوصول إلى إدارة النشر**:
   - انقر على "إدارة النشر" في القائمة الجانبية
   - ستظهر جميع الأبحاث المكتملة

2. **نشر بحث**:
   - اختر البحث المناسب
   - انقر على "نشر في المعرض" للنشر العادي
   - أو "ترشيح كبحث مميز" للأبحاث الاستثنائية

3. **إدارة الأبحاث المنشورة**:
   - يمكن جعل بحث منشور "مميزاً" لاحقاً
   - يمكن إزالة التمييز
   - يمكن إلغاء النشر نهائياً

---

## 🔔 نظام الإشعارات

عند نشر بحث، يتلقى الطالب إشعاراً تلقائياً:

**للنشر العادي**:

```
✅ تم نشر بحثك
تم نشر بحثك "[عنوان البحث]" في معرض مداد.
الآن يمكن للطلاب الآخرين الاستفادة منه.
```

**للبحث المميز**:

```
🌟 بحثك تم ترشيحه كبحث مميز!
تهانينا! تم ترشيح بحثك "[عنوان البحث]" كبحث مميز في معرض مداد.
هذا إنجاز رائع!
```

---

## 🎨 التصميم والواجهة

### صفحة المعرض:

- **Header**: خلفية تدرجية جميلة مع إحصائيات
- **Filters**: بحث + تصفية حسب السنة + عرض المميزة فقط
- **Grid**: تخطيط شبكي responsive (1-3 أعمدة)
- **Cards**: بطاقات أنيقة مع hover effects
- **Modal**: نافذة تفاصيل منبثقة كاملة

### صفحة إدارة النشر:

- **Dashboard**: لوحة تحكم بإحصائيات
- **List**: قائمة أبحاث مع أزرار واضحة
- **Badges**: علامات للحالة (منشور، مميز)
- **Actions**: أزرار ملونة حسب الوظيفة

---

## 🔐 الأمان

1. **RLS Policies**: التحكم الكامل في الوصول على مستوى قاعدة البيانات
2. **Role-based**: صفحة إدارة النشر للمشرفين فقط
3. **Validation**: التحقق من اكتمال البحث قبل النشر
4. **Ownership**: يمكن للطلاب رؤية أبحاثهم فقط (غير المنشورة)

---

## 📊 الإحصائيات المتاحة

### في صفحة المعرض:

- عدد الأبحاث المنشورة
- عدد الأبحاث المميزة
- متوسط عدد الكلمات

### في صفحة إدارة النشر:

- عدد الأبحاث المكتملة
- عدد المنشورة
- عدد المميزة

---

## 🔄 تطبيق التحديثات

### 1. تحديث قاعدة البيانات:

```bash
# في Supabase Dashboard > SQL Editor
# تنفيذ الملف: supabase/schema.sql
```

أو يمكنك تنفيذ الأوامر التالية:

```sql
-- إضافة الحقول الجديدة
ALTER TABLE research_projects ADD COLUMN summary TEXT;
ALTER TABLE research_projects ADD COLUMN is_published BOOLEAN DEFAULT FALSE;
ALTER TABLE research_projects ADD COLUMN is_featured BOOLEAN DEFAULT FALSE;
ALTER TABLE research_projects ADD COLUMN published_at TIMESTAMP WITH TIME ZONE;
ALTER TABLE research_projects ADD COLUMN supervisor_id UUID REFERENCES users(id);
ALTER TABLE research_projects ADD COLUMN supervisor_name TEXT;
ALTER TABLE research_projects ADD COLUMN graduation_year INTEGER;

-- إضافة السياسات
CREATE POLICY "Users can view published research projects"
ON research_projects FOR SELECT USING (is_published = TRUE);

CREATE POLICY "Supervisors can update assigned research projects"
ON research_projects FOR UPDATE USING (auth.uid() = supervisor_id);

-- إضافة الفهارس
CREATE INDEX idx_research_projects_published
ON research_projects(is_published) WHERE is_published = TRUE;

CREATE INDEX idx_research_projects_featured
ON research_projects(is_featured) WHERE is_featured = TRUE;
```

### 2. إعادة تشغيل التطبيق:

```bash
npm run dev
```

---

## 📝 ملاحظات مهمة

1. **شرط النشر**: يجب أن يكون البحث في حالة "completed" قبل النشر
2. **الملخص**: يُفضل إضافة ملخص للبحث قبل النشر (اختياري)
3. **السنة**: يتم تعيين سنة التخرج تلقائياً عند النشر (السنة الحالية)
4. **المشرف**: يتم تعيين معرف واسم المشرف تلقائياً من بيانات المستخدم

---

## 🚀 التطويرات المستقبلية المقترحة

1. **تقييم الأبحاث**: إضافة نظام تقييم بالنجوم
2. **التعليقات**: السماح بالتعليقات على الأبحاث
3. **التصدير**: تصدير الأبحاث كملفات PDF
4. **البحث المتقدم**: بحث في المحتوى الكامل
5. **الفئات**: تصنيف الأبحاث حسب المجال
6. **الإحصائيات**: عدد المشاهدات لكل بحث
7. **المشاركة**: مشاركة الأبحاث عبر روابط
8. **الأرشيف**: أرشفة الأبحاث القديمة

---

## ✅ الخلاصة

تم إضافة نظام كامل ومتكامل لمعرض الأبحاث يشمل:

✅ قاعدة بيانات محدثة بحقول وسياسات جديدة
✅ صفحة معرض جذابة للطلاب
✅ لوحة تحكم للمشرفين لإدارة النشر
✅ نظام تصفية وبحث متقدم
✅ نظام إشعارات تلقائي
✅ تصميم responsive وأنيق
✅ أمان على مستوى الصفوف (RLS)
✅ توثيق كامل وشامل

**النتيجة**: مداد الآن ليس مجرد أداة للكتابة، بل منصة معرفية تربط الأجيال! 🎓✨

---

## 📞 الدعم

إذا واجهت أي مشاكل أو كانت لديك أسئلة:

1. راجع هذا الملف التوثيقي
2. تحقق من console logs في المتصفح
3. تحقق من Supabase logs
4. تواصل مع فريق التطوير

---

**تم التطوير بواسطة**: GitHub Copilot 🤖
**التاريخ**: فبراير 2026
**الإصدار**: 1.0.0
