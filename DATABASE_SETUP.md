# دليل ربط قاعدة البيانات مع Vercel

## خطوات الإعداد الكاملة 🚀

---

## 📋 المتطلبات

- ✅ حساب Supabase (مجاني)
- ✅ حساب Vercel (مجاني)
- ✅ مشروعك موجود على GitHub

---

## 🗄️ الخطوة 1: إنشاء قاعدة بيانات Supabase

### 1. إنشاء مشروع جديد

1. اذهب إلى [https://supabase.com](https://supabase.com)
2. قم بتسجيل الدخول أو إنشاء حساب
3. اضغط **"New Project"**
4. املأ البيانات:
   - **Name**: `medad-research-system`
   - **Database Password**: كلمة مرور قوية (احفظها!)
   - **Region**: اختر أقرب منطقة (مثل: Frankfurt)
5. انتظر حتى ينتهي الإنشاء (2-3 دقائق)

### 2. تنفيذ SQL Schema

1. من لوحة التحكم، اذهب إلى **SQL Editor** (من القائمة اليمنى)
2. اضغط **"New Query"**
3. افتح ملف `supabase/schema.sql` من مشروعك
4. انسخ **كل** المحتوى والصقه في محرر SQL
5. اضغط **"Run"** أو `Ctrl + Enter`
6. تحقق من ظهور رسالة **"Success"** باللون الأخضر

### 3. الحصول على مفاتيح API

1. اذهب إلى **Settings** → **API** (من القائمة)
2. ستجد:
   - **Project URL**: `https://xxxxxxxxxx.supabase.co`
   - **anon/public key**: مفتاح طويل يبدأ بـ `eyJhbG...`
3. احفظ هذه القيم - ستحتاجها لاحقاً

---

## 🔐 الخطوة 2: إعداد المتغيرات البيئية محلياً

### 1. إنشاء ملف `.env.local`

في مجلد المشروع، أنشئ ملف باسم `.env.local`:

```bash
# Windows PowerShell
Copy-Item .env.local.example .env.local
```

```bash
# أو يدوياً
# انسخ محتوى .env.local.example إلى ملف جديد .env.local
```

### 2. ملء المفاتيح

افتح `.env.local` وضع قيمك:

```env
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Google AI (اختياري - للذكاء الاصطناعي)
GOOGLE_AI_API_KEY=AIzaSy...
```

### 3. اختبار محلياً

```bash
npm run dev
```

سجل حساب جديد وتحقق من عمل كل شيء!

---

## ☁️ الخطوة 3: الربط مع Vercel

### الطريقة الأولى: من لوحة Vercel (مباشرة)

1. اذهب إلى [https://vercel.com/dashboard](https://vercel.com/dashboard)
2. اختر مشروعك
3. اذهب إلى **Settings** → **Environment Variables**
4. أضف المتغيرات واحداً تلو الآخر:

| **Name**                        | **Value**                        |
| ------------------------------- | -------------------------------- |
| `NEXT_PUBLIC_SUPABASE_URL`      | `https://xxxxxxxxxx.supabase.co` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `eyJhbGciOiJIUzI1NiIsInR5cCI...` |
| `GOOGLE_AI_API_KEY`             | `AIzaSy...` (اختياري)            |

5. اضغط **Save**

### الطريقة الثانية: من Supabase (تكامل تلقائي)

1. من مشروع Supabase → **Settings** → **Integrations**
2. ابحث عن **Vercel**
3. اضغط **Connect**
4. اختر مشروعك من Vercel
5. سيتم إضافة المتغيرات **تلقائياً**! ✨

---

## 🚀 الخطوة 4: نشر التحديثات

### إعادة نشر المشروع

```bash
# 1. Commit التغييرات
git add .
git commit -m "Add Supabase database integration"
git push origin main

# 2. Vercel سيقوم بالنشر تلقائياً!
```

أو من لوحة Vercel → **Deployments** → **Redeploy**

---

## ✅ الخطوة 5: التحقق من النجاح

### 1. اختبار التسجيل

1. اذهب إلى موقعك على Vercel: `https://your-site.vercel.app`
2. اذهب إلى صفحة التسجيل `/register`
3. سجل حساب جديد
4. تحقق من تسجيل الدخول تلقائياً

### 2. التحقق من قاعدة البيانات

1. ارجع إلى Supabase
2. **Table Editor** → `users`
3. يجب أن ترى المستخدم الجديد! 🎉

### 3. اختبار متعدد الأجهزة

1. سجل دخول من متصفح آخر
2. سجل دخول من جهاز آخر (موبايل مثلاً)
3. يجب أن **تظهر نفس البيانات** في كل مكان! ✅

---

## 🔧 استكشاف الأخطاء

### ❌ خطأ: "Missing Supabase environment variables"

**الحل:**

- تأكد من وجود ملف `.env.local` محلياً
- تأكد من إضافة المتغيرات في Vercel
- أعد نشر المشروع

### ❌ خطأ: "User already registered"

**الحل:**

- البريد الإلكتروني مستخدم بالفعل
- استخدم بريد إلكتروني آخر
- أو احذف المستخدم من Supabase → Table Editor

### ❌ خطأ: "Invalid credentials"

**الحل:**

- تأكد من كلمة المرور الصحيحة
- تأكد من تفعيل البريد الإلكتروني (إذا كان مفعلاً في Supabase)

### ❌ الجداول غير موجودة

**الحل:**

```sql
-- نفذ هذا في SQL Editor
DROP SCHEMA public CASCADE;
CREATE SCHEMA public;

-- ثم نفذ schema.sql مرة أخرى
```

---

## 📊 مميزات قاعدة البيانات

✅ **تسجيل دخول من أي جهاز** - بياناتك محفوظة في السحابة  
✅ **أمان عالي** - Row Level Security (RLS)  
✅ **نسخ احتياطي تلقائي** - Supabase يحفظ نسخ تلقائية  
✅ **سرعة فائقة** - PostgreSQL مع فهرسة محسّنة  
✅ **مجاني تماماً** - حتى 500MB بيانات و 50,000 مستخدم

---

## 🆘 الدعم

إذا واجهت أي مشكلة:

1. تحقق من **Logs** في Vercel: `Deployments` → `Functions` → اختر Log
2. تحقق من **Logs** في Supabase: `Logs` → `Explorer`
3. راجع التوثيق:
   - [Supabase Docs](https://supabase.com/docs)
   - [Vercel Docs](https://vercel.com/docs)

---

## 🎉 تم بنجاح!

الآن موقعك:

- ✅ متصل بقاعدة بيانات حقيقية
- ✅ يعمل من أي جهاز
- ✅ آمن ومحمي
- ✅ جاهز للاستخدام الفعلي

**استمتع بمشروعك! 🚀**
